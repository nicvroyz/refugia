services:
  postgres:
    image: postgres:16-alpine
    container_name: nannyconnect_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-nannyuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nannypassword}
      POSTGRES_DB: ${POSTGRES_DB:-nannyconnect}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-nannyuser}"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nannyconnect_app
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-nannyuser}:${POSTGRES_PASSWORD:-nannypassword}@postgres:5432/${POSTGRES_DB:-nannyconnect}
    env_file:
      - .env
    ports:
      - "3000:3000"

  nginx:
    image: nginx:alpine
    container_name: nannyconnect_nginx
    restart: unless-stopped
    depends_on:
      - app
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_data:
    driver: local
